pipeline {
    agent any
    
    stages {
        stage("build") {
            steps{
                echo "this build"
            }
        }
        stage("deploy") {
            steps{
                echo "this deploy"
            }
        
        }
    }
}
